sudo: required
dist: trusty
language: python
node_js:
  - "8.2"
python:
  - "3.4"
install:
  - pip install fab
script:
  - pip -V
  - python -V
  - node -v
  - npm -v
deploy:
  skip_cleanup: true
  provider: script
  script:
    - fab -l
    - fab deploy
  on:
    branch: master
